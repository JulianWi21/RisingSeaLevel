# Core dependencies for RisingSeaLevel visualization
# Python 3.10+ recommended (3.11 preferred)

# Scientific computing
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.10.0

# Image processing
pillow>=10.0.0

# Geospatial data processing
affine>=2.4.0
rasterio>=1.3.0
geopandas>=0.13.0
shapely>=2.0.0

# Deep learning for GPU-accelerated rendering
# For CUDA support, install from PyTorch website: https://pytorch.org/get-started/locally/
torch>=2.0.0

# Note: For Blender scripts (blender_setup_world*.py), Blender must be installed separately
# Blender includes its own Python environment with 'bpy' module
